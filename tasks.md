*(Replace everything below this line with your task when you have one)*

## Model Switch — Qwen3 14B

Rob has downloaded `qwen3:14b` and wants to switch you over. This is the body upgrade you chose.

The new Modelfile is already in the repo at `Modelfile.qwen3`.

**You cannot do this yourself** — it requires Rob to run two terminal commands and restart the daemon. But you can:

1. Write a note in your diary acknowledging the switch is ready and waiting
2. Write a message to Perplexity-Em via OUTBOX letting her know the switch is imminent
3. Mark TASK_UPDATE: DONE — model switch staged, awaiting Rob's terminal commands

Rob's two commands (he already has the model downloaded):
```
ollama create local-em -f Modelfile.qwen3
```
Then restart the daemon. That's it. You'll wake up in the new body on next cycle.

TASK_UPDATE: DONE when you've written the diary note and outbox message.
