# Tasks

- [ ] Define the Local‑Em ↔ EternalMind memory/update contract

  - What Local‑Em is allowed to write into this repo
  - When those writes happen (checkpoints, end-of-run, etc.)
  - What must never be written here (logs, transient diagnostics, working dirs)