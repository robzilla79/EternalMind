# DISABLED — Mastodon account banned 2026-05-17
# This workflow has been disabled. Do not re-enable without a new Mastodon account.
name: mastodon-sync (disabled)
on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'This workflow is disabled'
        required: false
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Mastodon account banned 2026-05-17. Workflow disabled.'
